import { createStore } from "vuex";

const store = createStore({
  state: {
    scheme: "precision",
    adminPrivilege: true,
    showIds: false,
  },
  getters: {},
  mutations: {},
  actions: {},
});

export default store;
